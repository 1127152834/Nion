# Workspace AGENTS

本文件补充当前仓库根目录下的执行约束，作用域覆盖整个仓库；`backend/AGENTS.md` 与 `frontend/AGENTS.md` 的更深层规则继续生效。

## 当前硬规则

### 1. 禁止补丁叠补丁

- 遇到边界错误、职责错误、信息架构错误时，优先重构或删除，不要继续包兼容层、转发层、临时壳。
- 如果一个模块已经明显失控，不要围绕旧结构修补；直接按批准的设计重切边界。
- 可以大刀阔斧地删除废弃页面、旧路由、空壳组件、补丁测试、兼容入口，不要犹豫。

### 2. 代码质量优先于“改动小”

- 允许为了边界清晰而做较大改动。
- 允许为了职责清晰而重命名、拆分、合并文件。
- 不要为了保持旧接口表面稳定而牺牲模型正确性，除非计划中明确要求临时兼容。
- 所有实现必须以“长期可维护”为第一优先级，而不是“先跑起来”。

### 3. 禁止 Proposal 语义

- `memory`、`soul`、以及其他模块中都不再允许存在 `proposal / accept / reject` 这一套产品语义。
- 只允许两种决策来源：
  - `agent` 直接决定并执行
  - `user` 明确决定
- 新代码、新事件名、新 API、新测试、新文档都不得再引入 proposal 概念。
- 旧的 proposal 相关代码、路由、事件、测试在改造到位后应删除或退休。

### 4. 当前执行必须服从已批准设计

- 当前 Memory / Soul 改造必须服从：
  - `docs/superpowers/specs/2026-04-09-memory-soul-boundary-contracts-design.md`
  - `docs/superpowers/plans/2026-04-09-memory-soul-boundary-contracts-implementation-plan.md`
- 不要跳过 Phase 0/1 直接写产品面代码。
- 不要从旧 review 文档、旧页面、旧合同里反推新设计。

### 5. 当前改造默认策略

- 先重写合同与测试，再改实现。
- 先切用户面与内部治理面的边界，再删旧 UI。
- 先让前端停止消费错误合同，再清 compat 与旧壳。
- 任何发现的死代码、空壳、未接主链组件，只要不再服务当前设计，都应删除。
