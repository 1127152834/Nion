{
  "name": "nion-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/main/index.js",
    "test": "node --test tests/*.test.mjs",
    "package:builder": "node -e \"console.log('electron-builder packaging will be wired in a later task')\"",
    "package:forge": "node -e \"console.log('electron-forge packaging will be wired in a later task')\"",
    "build:helper": "node -e \"console.log('python helper build will be wired in a later task')\""
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.8.2"
  }
}
